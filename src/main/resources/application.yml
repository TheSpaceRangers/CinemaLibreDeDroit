spring:
  application:
    name: Test

  # Configuration de la source de donn�es MySQL
  datasource:
    url: jdbc:mysql://localhost:3306/cinemasansdroit
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Configuration de JPA (Java Persistence API)
  jpa:
    hibernate.ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true